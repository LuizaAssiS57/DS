1. Em qual classe foi utilizado encapsulamento?
O encapsulamento foi utilizado nas classes Pedido e PedidoDelivery, e em Curso e CursoOnline, onde os atributos foram declarados como private e acessados através dos métodos get e set que são públicos. (atividade5) (atividade4)

2. Quais classes participaram da herança?
As classes Veiculo, Carro e Moto (atividade2).

3. Qual classe foi considerada a classe pai?
A classe Veiculo (atividade2).

4. Qual ou quais classes foram consideradas classes filhas?
Carro e Moto (atividade2).

5. Qual interface foi criada?
A interface Pagamento (atividade1).

6. Qual classe utilizou implements?
A classe abstrata Pedido utilizou o implements Pagamento (atividade5).

7. Qual método possui sobrecarga?
O método realizarPagamento (atividade5).

8. Qual é a diferença entre os métodos sobrecarregados?
A diferença está nos parâmetros, o primeiro método recebe apenas um parâmetro, o segundo recebe dois parâmetros, e o terceiro recebe dois parâmetros de tipos diferentes.

9. Onde foi utilizado extends?
Foi utilizado na declaração das classes filhas para estabelecer a herança.
Ex: public class PedidoLocal extends Pedido e public class PedidoDelivery extends Pedido.
(atividade5)

10. Explique com suas palavras a função de uma interface em Java.
É como definir um contrato que determina quais métodos uma classe deve implementar, especificando o que deve ser feito, mas sem definir como isso será feito.